# App Icons

Place icon files here for desktop builds:

- `icon.icns` — macOS (1024x1024)
- `icon.ico` — Windows (256x256)
- `icon.png` — Source image (1024x1024)

electron-builder picks these up automatically from this directory.
